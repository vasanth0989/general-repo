ach.injectOpen(
    // Single spike — this alone reproduces TaskRejectedException
    atOnceUsers(50),

    // Sustained load to observe app behavior post-spike
    constantUsersPerSec(CONSTANTUSERPERSEC_USER).during(DURATION_TIME),

    // Clean ramp down
    rampUsersPerSec(TPS_USER).to(0).during(DURING_TIME)
).protocols(httpProtocol_1)
